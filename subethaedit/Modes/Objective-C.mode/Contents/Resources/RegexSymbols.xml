<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE syntax SYSTEM "symbol.dtd">
<symbols>

    <blocks>
        <beginregex>{</beginregex>
        <endregex>}</endregex>
    </blocks>
    
    <symbol id="Objective-C Methods" font-weight="normal" font-style="normal" image="SymbolM" indentation="1" ignoreblocks="yes">
        <regex>([-+][^(-;]*\([A-Za-z0-9 *_]*\)[A-Za-z0-9_ ]+[^{;]*)</regex>
        <postprocess>
            <find>(:) *\([^\)]*\) *[a-zA-Z0-9]*</find>
            <replace>\1</replace>
            <find>([\n\r]| +|\([^\)]*\))</find>
            <replace> </replace>
            <find>^([-+]) *</find>
            <replace>\1</replace>
        </postprocess>
    </symbol>

    <symbol id="Objective-C Directives" font-weight="normal" font-style="normal" image="SymbolC" indentation="0" ignoreblocks="yes">
        <regex>(@implementation[^[:cntrl:]{]*|@interface[^[:cntrl:]{]*|@protocol[^[:cntrl:]{]*)</regex>
        <postprocess>
            <find>([\n\r]| +)</find>
            <replace> </replace>
        </postprocess>
    </symbol>
    
    <symbol id="Pragma Mark Separators" font-weight="normal" font-style="normal" image="SymbolC" indentation="0" ignoreblocks="yes">
        <regex>(#pragma mark[	 -]*[^\n]*)\n</regex>
        <postprocess>
            <find>#pragma mark[-	 \n]*</find>
            <replace></replace>
        </postprocess>
    </symbol>
</symbols>