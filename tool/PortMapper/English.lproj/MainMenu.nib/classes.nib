{
    IBClasses = (
        {
            ACTIONS = {check = id; map = id; }; 
            CLASS = Controller; 
            LANGUAGE = ObjC; 
            OUTLETS = {portTextField = NSTextField; statusTextField = NSTextField; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}