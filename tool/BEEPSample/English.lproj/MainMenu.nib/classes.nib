{
    IBClasses = (
        {
            ACTIONS = {connect = id; sendMessage = id; toggleListener = id; }; 
            CLASS = Controller; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                "O_connectButton" = NSButton; 
                "O_listenerControlButton" = NSButton; 
                "O_messageTextField" = NSTextField; 
                "O_peerAddressTextField" = NSTextField; 
                "O_ports" = NSTextField; 
                "O_receivedTextView" = NSTextView; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}