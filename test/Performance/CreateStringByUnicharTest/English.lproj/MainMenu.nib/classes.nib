{
    IBClasses = (
        {
            ACTIONS = {testIt = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {"O_numberOfStringsTextField" = NSTextField; "O_outputTextView" = NSTextView; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}